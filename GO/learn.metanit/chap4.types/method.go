package main

/*
import (
	"fmt"
)

type library []string

// func (l ТИП) ИМЯ МЕТОДА() (здесь тип возвращаемого элемента) {
func (l library) print() {

	for _, val := range l {
		fmt.Println(val)
	}
}

var lib library = library{"First book", "Second book", "Third book"}

///////////////////////////////

type person struct {
	name string
	age  int
}

func (p person) info() {
	fmt.Println("Имя:", p.name)
	fmt.Println("Возраст:", p.age)
}

func (p person) eat(meal string) {
	fmt.Println(p.name, "ест", meal)
}

func main() {
	lib.print()
	//First book
	//Second book
	//Third book

	fmt.Println(lib)
	//[First book Second book Third book]

	var tom = person{name: "Tom", age: 24}
	tom.info()
	//Имя: Tom
	//Возраст: 24

	tom.eat("борщ с капустой, но не красный")
	//Tom ест борщ с капустой, но не красный
}
*/
